# 2026.09.19 并发与数据考取检查记录

- 单目录大量文件不再一个文件建立一次连接，改为按大小均衡分配给最多 3 条持续连接。
- 多个一级文件夹保持目录边界，同一文件夹不会拆给多个并发连接，输出保留完整相对路径。
- 外层已经并发时，内部任务顺序执行，避免连接数叠加；本地来源顺序模式确保处理全部任务组。
- 并发任务失败后只重试失败文件；成功文件不会重复下载。
- 最终校验失败会生成 `incomplete_时间.json`，方便定位和再次考取，不含账号或密码。

本机共有 66 项测试通过。并发专项测试包含 120 个文件内容逐一比对、只建立 3 个 SFTP 会话、目录分组唯一性、单文件断线重试和本地来源全部任务组检查。现场服务器连接数限制和实际吞吐仍需在现场网络验证。
